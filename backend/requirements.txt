fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
langchain>=0.3.0
langchain-openai>=0.2.0
sqlalchemy>=2.0.35
httpx>=0.27.0
pytest>=8.3.0

# Optional — install only if you need them:
#   psycopg2-binary>=2.9.9          # PostgreSQL driver (SQLite is the default)
#   transformers>=4.44.0 torch      # Phase 2: local HuggingFace models
