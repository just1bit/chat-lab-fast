fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
langchain>=0.3.0
langchain-openai>=0.2.0
sqlalchemy>=2.0.35
httpx>=0.27.0
pytest>=8.3.0

# Optional — install for local HuggingFace model inference (Phase 2):
#   pip install transformers torch
# Then set ENABLE_LOCAL_MODELS=true in .env
